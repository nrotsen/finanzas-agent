[pytest]
# `tests/` es un paquete (tiene __init__.py), así que pytest agrega la raíz del
# repo a sys.path y `from src...` resuelve sin instalar nada ni tocar
# PYTHONPATH. No hace falta más configuración que decirle dónde mirar.
testpaths = tests
addopts = -q --strict-markers
